import { PositiveNumberFormControl } from './positive-number-form-control';

describe('PositiveNumberFormControl', () => {
  it('should create an instance', () => {
    expect(new PositiveNumberFormControl()).toBeTruthy();
  });
});
