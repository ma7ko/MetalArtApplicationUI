import { LetterFormControl } from './letter-form-control';

describe('LetterFormControl', () => {
  it('should create an instance', () => {
    expect(new LetterFormControl()).toBeTruthy();
  });
});
