import { ZipCodeFormControl } from './zip-code-form-control';

describe('ZipCodeFormControl', () => {
  it('should create an instance', () => {
    expect(new ZipCodeFormControl()).toBeTruthy();
  });
});
