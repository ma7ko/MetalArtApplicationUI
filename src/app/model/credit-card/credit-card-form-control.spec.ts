import { CreditCardFormControl } from './credit-card-form-control';

describe('CreditCardFormControl', () => {
  it('should create an instance', () => {
    expect(new CreditCardFormControl()).toBeTruthy();
  });
});
