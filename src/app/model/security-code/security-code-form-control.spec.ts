import { SecurityCodeFormControl } from './security-code-form-control';

describe('SecurityCodeFormControl', () => {
  it('should create an instance', () => {
    expect(new SecurityCodeFormControl()).toBeTruthy();
  });
});
